#!/bin/sh

[ -d $HOME/.dotfiles ] && echo '~/.dotfiles already exists' && exit 1

set -eux

git init --bare $HOME/.dotfiles

alias dotfiles="git --git-dir=${HOME}/.dotfiles/ --work-tree=${HOME}"

dotfiles config --local status.showUntrackedFiles no
