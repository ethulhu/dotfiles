#!/bin/sh

set -eu

if [ $# -eq 1 ] && [ "${1}" = '-h' ]; then
  echo 'Usage:'
  echo "    $(basename "${0}")"
  echo
  echo 'Synopsis:'
  echo '    Lists currently deliberately installed packages (not dependencies).'
  exit
fi

brew leaves
