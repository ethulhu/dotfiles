#!/bin/sh

set -eu

function usage() {
  echo "Usage: $(basename "${0}") <wifi-SSID>"
  echo
  echo 'Synopsis: Return 0 if connected to the given wifi network, 1 otherwise.'
}

if [ $# -ne 1 ]; then
  usage
  exit 1
fi

networksetup -getairportnetwork en0 | grep -q "Current Wi-Fi Network: ${1}"
